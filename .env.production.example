# ==============================================
# PRODUCTION ENVIRONMENT VARIABLES
# For Vercel + Render + Render PostgreSQL Setup
# ==============================================

# -----------------
# BACKEND (.env on Render)
# -----------------

# Node Environment
NODE_ENV=production

# PostgreSQL Database Connection
# Get this from: Render Dashboard → Your Database → Info → Internal Database URL
DATABASE_URL=postgres://loomio_user:password@dpg-xxxx.oregon-postgres.render.com/loomio_db

# Individual Database Components (optional if using DATABASE_URL)
DB_HOST=dpg-xxxx.oregon-postgres.render.com
DB_USER=loomio_user
DB_PASSWORD=your_database_password
DB_NAME=loomio_db
DB_PORT=5432

# JWT Configuration
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this
JWT_EXPIRES_IN=7d

# Session Secret
SESSION_SECRET=your-super-secret-session-key-min-32-chars-change-this

# CORS Configuration
# Update this with your actual Vercel URL after frontend deployment
FRONTEND_URL=https://your-app-name.vercel.app

# Server Configuration
PORT=10000

# -----------------
# FRONTEND (.env on Vercel)
# -----------------

# Backend API URL
# Update this with your actual Render backend URL
VITE_API_URL=https://your-backend-name.onrender.com

# ==============================================
# QUICK SETUP GUIDE
# ==============================================
#
# 1. Create Render PostgreSQL database
# 2. Copy DATABASE_URL from Render
# 3. Generate JWT_SECRET and SESSION_SECRET
# 4. Deploy backend to Render with these vars
# 5. Deploy frontend to Vercel with VITE_API_URL
# 6. Update FRONTEND_URL in Render
#
# ==============================================

# HELPFUL COMMANDS
# ==============================================
#
# Generate Secrets:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Initialize Database (in Render Shell):
# node -e "require('./src/config/database').sync({force:false})"
#
# Test Database Connection (in Render Shell):
# node -e "require('./src/config/database').authenticate().then(() => console.log('Connected!'))"
#
# ==============================================

